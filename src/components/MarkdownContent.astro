---
interface Props {
    className?: string;
}

const { className } = Astro.props;
---
<main class:list={["prose prose-lg max-w-full lg:prose-xl dark:prose-invert lg:max-w-[750px]", className]}>
    <slot />
</main>
